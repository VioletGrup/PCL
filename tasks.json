{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ruff Fix + Format",
            "type": "shell",
            "command": "ruff check . --fix && ruff format .",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}