/**
 * Global configuration for the PCL Earthworks Analysis Tool Frontend.
 */
const config = {
    // Base URL for the backend API.
    // Default to localhost:8000 for local development.
    // In production, this can be replaced or injected via environment variables.
    API_BASE_URL: "http://127.0.0.1:8000",
};

export default config;
